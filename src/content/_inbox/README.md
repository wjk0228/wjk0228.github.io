# 收件箱

这里用于保存尚未整理的临时记录，不参与网站构建。

整理完成后：

1. 从 `_templates` 插入对应模板。
2. 补齐标题、摘要、领域、主题和标签。
3. 保持 `draft: true` 进行本地检查。
4. 将文件移动到正式内容目录。
5. 确认可以公开后改为 `draft: false`。

不要在公开笔记中记录客户身份信息、银行内部数据、敏感制度或未经授权的材料。
